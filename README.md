# Fatality.win-Trashtalk-lua
Automatically says a message in chat when you get a kill. You can choose between triggering on all kills or only on headshots and one-taps. Optionally, you can set a custom message instead of the default 1
